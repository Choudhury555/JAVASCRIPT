const { logInfo, logError } = require("./p206_logger.cjs");

logInfo("Server started on port 3000");//ℹ️ Server started on port 3000
logError("Database connection failed!");//❌ Database connection failed!